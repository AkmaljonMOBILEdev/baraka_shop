import 'package:flutter/material.dart';

class AdCategoryScreen extends StatelessWidget {
  const AdCategoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
